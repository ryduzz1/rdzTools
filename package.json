{
  "name": "rdztools",
  "version": "0.1.0",
  "private": true,
  "description": "Starter After Effects CEP panel scaffold for animation and look presets.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "node scripts/build.mjs --clean",
    "package": "node scripts/package.mjs"
  }
}
